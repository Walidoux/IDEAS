# Examples:

```csharp
on first join:
on room enter/leave:
on reion enter/leave:
on tile click:
on vehicle enter:
on sit:
on lay:
on move:

every 2 seconds:
every hour: // will countdown be reset ?
every 10 minutes:
```
